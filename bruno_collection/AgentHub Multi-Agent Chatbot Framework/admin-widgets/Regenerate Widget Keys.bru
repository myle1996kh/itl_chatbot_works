meta {
  name: Regenerate Widget Keys
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/admin/tenants/:tenant_id/widget/regenerate-keys
  body: none
  auth: bearer
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}
