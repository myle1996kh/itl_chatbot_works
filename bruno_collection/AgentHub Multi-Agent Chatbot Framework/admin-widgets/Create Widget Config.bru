meta {
  name: Create Widget Config
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/admin/tenants/:tenant_id/widget
  body: none
  auth: bearer
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}
