meta {
  name: Change Password
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "old_password": "",
    "new_password": ""
  }
}
