meta {
  name: Delete Documents
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/admin/tenants/:tenant_id/knowledge
  body: json
  auth: bearer
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  [
    {}
  ]
}
