meta {
  name: Get Staff
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/admin/tenants/:tenant_id/staff
  body: none
  auth: bearer
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}
