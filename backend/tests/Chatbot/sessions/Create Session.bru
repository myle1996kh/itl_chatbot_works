meta {
  name: Create Session
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/:tenant_id/sessions
  body: json
  auth: bearer
}

params:query {
  user_id: 
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}
