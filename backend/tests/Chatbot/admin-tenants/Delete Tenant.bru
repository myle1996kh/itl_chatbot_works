meta {
  name: Delete Tenant
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/admin/tenants/:tenant_id
  body: none
  auth: bearer
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}
