meta {
  name: Update Tenant Permissions
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/admin/tenants/:tenant_id/permissions
  body: json
  auth: bearer
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "agent_permissions": "",
    "tool_permissions": ""
  }
}
