meta {
  name: Get Tenant
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/admin/tenants/:tenant_id
  body: none
  auth: bearer
}

params:path {
  tenant_id: 
}

auth:bearer {
  token: {{token}}
}
