meta {
  name: List Agents
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/admin/agents
  body: none
  auth: bearer
}

params:query {
  ~is_active: 
  ~limit: 
  ~offset: 
}

auth:bearer {
  token: {{token}}
}
